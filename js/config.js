// Configuración opcional para despliegues estáticos (por ejemplo GitHub Pages).
// Deje vacío para usar /api en el mismo origen. Si el backend vive en otro host,
// establezca aquí su URL base HTTPS, sin /api al final.
window.CVISADOS_BACKEND_URL=window.CVISADOS_BACKEND_URL||'';
